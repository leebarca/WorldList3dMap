<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.leebarcaglobal.worldtravel3d">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorldList3dApp"
        tools:targetApi="31">

        <!-- SplashActivity with launch theme -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.WorldList3dApp.Launch"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CountryDetailActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CountryDetailsAttractions" />
        <activity android:name=".CountryDetailsInfo" />
        <activity android:name=".CountryDetailsPlanning" />
        <activity android:name=".ExploreActivity" />
        <activity android:name=".LanguageActivity" />
        <activity android:name=".TripPlannerActivity" />
        <activity android:name=".AddTrip" />
        <activity android:name=".ViewEditTripActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>